.DS_Store
.claude/
.wrangler/

# Dossier de travail de la refonte (brief interne + PNG sources 21 Mo).
# Exclu volontairement : Cloudflare Pages publie tout le dépôt, ce dossier
# serait donc accessible en ligne. Les visuels retenus vivent dans assets/app/.
refonte/
